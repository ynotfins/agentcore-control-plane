{
  "mcpServers": {
    "agentcore-gateway": {
      "url": "http://127.0.0.1:8080/mcp",
      "headers": {
        "Authorization": "Bearer ${env:BIFROST_MCP_VIRTUAL_KEY}"
      },
      "disabled": false,
      "timeout": 300
    }
  },
  "_agentcore": {
    "renderer": "zoo-code",
    "source_contract": "contracts/agentcore-gateway-client.json",
    "notes": [
      "Sanitized merge fragment; never commit or print the resolved virtual key.",
      "Zoo-Code persists MCP settings under Cursor globalStorage on this host. A governed live installer must materialize the Windows User env value into the live file only."
    ]
  }
}
