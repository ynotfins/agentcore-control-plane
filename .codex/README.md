# .codex — pointer

Codex follows the same canonical policy as all managed agents:

1. `AGENTS.md` (root agent contract — Codex reads this natively)
2. `rules/canonical/GLOBAL_AGENT_RULES.md` ← rendered from `contracts/global-agent-policy.yaml`
3. `docs/agent-policy/` (project execution standards)

Codex-specific IDE profile: `ide-profiles/codex/`. Do not add rule copies here that could drift from the canonical policy.
