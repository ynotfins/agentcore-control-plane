# .agents — pointer

Canonical agent policy for this repository:

1. `AGENTS.md` (root agent contract)
2. `rules/canonical/GLOBAL_AGENT_RULES.md` ← rendered from `contracts/global-agent-policy.yaml`
3. `docs/agent-policy/` (project execution standards)

Do not add rule copies here that could drift from the canonical policy.
