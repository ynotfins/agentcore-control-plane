# Install / Update — Codex (OpenAI)

> **DERIVED FILE — do not hand-edit.** Generated by `scripts/render_ide_rules.py`.

Installation mode: **generated_prompt**. Live IDE files are never edited without explicit operator
authorization, a timestamped backup, and post-change validation.

## Rules delivery

1. Canonical semantics: `contracts/global-agent-policy.yaml` → rendered `ide-profiles/codex/GLOBAL_RULES.md`.
2. Delivery mechanism for this IDE: yes (direct_write; live edits remain approval-gated by policy) — target: C:\Users\ynotf\.codex\AGENTS.md.
3. If delivery is `manual_import`/`generated_prompt`, hand the rendered GLOBAL_RULES.md content to the operator or paste it via the documented UI path. Do not skip rules silently.

## MCP configuration

1. Template: `ide-profiles/codex/MCP_CONFIG_TEMPLATE.toml` (derived from `renderers/gateway-clients/codex.json`).
2. Live target: `C:\Users\ynotf\.codex\config.toml` — exactly one `agentcore-gateway` entry.
3. Follow `docs/prompts/install-agentcore-gateway-in-ide.md` for the approved live-change procedure (backup first).
4. Env handling: bearer_token_env_var (no inline header env expansion; no generic timeout_seconds field).
5. Restart behavior: restart Codex session after config.toml or env-var changes.

## After any change

Run the checks in `ide-profiles/codex/VALIDATION.md` and record the date in `IDE_PROFILE.yaml` (`last_validation_date`).
