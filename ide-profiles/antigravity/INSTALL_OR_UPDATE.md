# Install / Update — Antigravity (Gemini)

> **DERIVED FILE — do not hand-edit.** Generated by `scripts/render_ide_rules.py`.

Installation mode: **generated_prompt**. Live IDE files are never edited without explicit operator
authorization, a timestamped backup, and post-change validation.

## Rules delivery

1. Canonical semantics: `contracts/global-agent-policy.yaml` → rendered `ide-profiles/antigravity/GLOBAL_RULES.md`.
2. Delivery mechanism for this IDE: unverified — target: unverified.
3. If delivery is `manual_import`/`generated_prompt`, hand the rendered GLOBAL_RULES.md content to the operator or paste it via the documented UI path. Do not skip rules silently.

## MCP configuration

1. Template: `ide-profiles/antigravity/MCP_CONFIG_TEMPLATE.json` (derived from `renderers/gateway-clients/antigravity.json`).
2. Live target: `C:\Users\ynotf\.gemini\config\mcp_config.json` — exactly one `agentcore-gateway` entry.
3. Follow `docs/prompts/install-agentcore-gateway-in-ide.md` for the approved live-change procedure (backup first).
4. Env handling: supports env headers (per gateway-client contract).
5. Restart behavior: restart client after MCP config changes.

## After any change

Run the checks in `ide-profiles/antigravity/VALIDATION.md` and record the date in `IDE_PROFILE.yaml` (`last_validation_date`).
