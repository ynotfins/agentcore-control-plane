# Install / Update — Eigent

> **DERIVED FILE — do not hand-edit.** Generated by `scripts/render_ide_rules.py`.

Installation mode: **generated_prompt**. Live IDE files are never edited without explicit operator
authorization, a timestamped backup, and post-change validation.

## Rules delivery

1. Canonical semantics: `contracts/global-agent-policy.yaml` → rendered `ide-profiles/eigent/GLOBAL_RULES.md`.
2. Delivery mechanism for this IDE: no (manual_import) — target: C:\Users\ynotf\.eigent\skills\agentcore-project-lifecycle\SKILL.md.
3. If delivery is `manual_import`/`generated_prompt`, hand the rendered GLOBAL_RULES.md content to the operator or paste it via the documented UI path. Do not skip rules silently.

## MCP configuration

1. Template: `ide-profiles/eigent/MCP_CONFIG_TEMPLATE.json` (derived from `renderers/gateway-clients/eigent.json`).
2. Live target: `C:\Users\ynotf\.eigent\mcp.json` — exactly one AgentCore entry named `agentcore-gateway`. The only allowed additional direct MCP entry is the Zoo-Code-owned companion approved by `AUTH-2026-08-20-ZOO_CODE_DIRECT_MCP_EXCEPTION`; do not remove it when present and do not infer support when the host cannot run Zoo-Code.
3. Follow `docs/prompts/install-agentcore-gateway-in-ide.md` for the approved live-change procedure (backup first).
4. Env handling: no env-placeholder expansion evidenced; materialize BIFROST_MCP_VIRTUAL_KEY from Windows User env into live config only.
5. Restart behavior: restart Eigent after MCP or Windows environment changes; fresh workforce task required for native validation.

## CLIENT-LOCAL EXECUTION SCOPE

The IDE running this prompt may inspect and modify only its own live
configuration, rules, agent settings, and backup.

Configuration examples for other IDEs are reference material only.

Do not inspect, back up, repair, restart, validate, or modify another IDE.

Cross-IDE reconciliation is a separate AgentCore control-plane task that
requires explicit operator authorization.

## After any change

Run the checks in `ide-profiles/eigent/VALIDATION.md` and record the date in `IDE_PROFILE.yaml` (`last_validation_date`).
