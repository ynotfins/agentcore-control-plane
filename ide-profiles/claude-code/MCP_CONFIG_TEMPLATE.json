{
  "mcpServers": {
    "agentcore-gateway": {
      "type": "http",
      "url": "http://127.0.0.1:8080/mcp",
      "headers": {
        "Authorization": "Bearer ${env:BIFROST_MCP_VIRTUAL_KEY}"
      },
      "timeout": 300
    }
  },
  "_agentcore": {
    "renderer": "claude-code",
    "source_contract": "contracts/agentcore-gateway-client.json",
    "notes": [
      "Merge into C:\\Users\\ynotf\\.claude.json mcpServers when present."
    ]
  },
  "_template_note": "DERIVED from renderers/gateway-clients/claude-code.json (source contract: contracts/agentcore-gateway-client.json). Do not hand-edit; regenerate from the contract."
}
