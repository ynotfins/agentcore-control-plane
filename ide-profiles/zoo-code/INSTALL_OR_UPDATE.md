# Install / Update — Zoo Code

> **DERIVED FILE — do not hand-edit.** Generated by `scripts/render_ide_rules.py`.

Installation mode: **direct_write**. Live IDE files are never edited without explicit operator
authorization, a timestamped backup, and post-change validation.

## Rules delivery

1. Canonical semantics: `contracts/global-agent-policy.yaml` → rendered `ide-profiles/zoo-code/GLOBAL_RULES.md`.
2. Delivery mechanism for this IDE: unverified — target: unverified.
3. If delivery is `manual_import`/`generated_prompt`, hand the rendered GLOBAL_RULES.md content to the operator or paste it via the documented UI path. Do not skip rules silently.

## MCP configuration

1. Template: `ide-profiles/zoo-code/MCP_CONFIG_TEMPLATE.json` (derived from `renderers/gateway-clients/zoo-code.json`).
2. Live target: `C:\Users\ynotf\AppData\Roaming\Cursor\User\globalStorage\zoocodeorganization.zoo-code\settings\mcp_settings.json` — exactly one `agentcore-gateway` entry.
3. Follow `docs/prompts/install-agentcore-gateway-in-ide.md` for the approved live-change procedure (backup first).
4. Env handling: no documented env-header expansion in persisted MCP JSON; materialize BIFROST_MCP_VIRTUAL_KEY from Windows User env into live config only.
5. Restart behavior: restart Cursor or open a fresh Zoo Code task after MCP changes.

## CLIENT-LOCAL EXECUTION SCOPE

The IDE running this prompt may inspect and modify only its own live
configuration, rules, agent settings, and backup.

Configuration examples for other IDEs are reference material only.

Do not inspect, back up, repair, restart, validate, or modify another IDE.

Cross-IDE reconciliation is a separate AgentCore control-plane task that
requires explicit operator authorization.

## After any change

Run the checks in `ide-profiles/zoo-code/VALIDATION.md` and record the date in `IDE_PROFILE.yaml` (`last_validation_date`).
