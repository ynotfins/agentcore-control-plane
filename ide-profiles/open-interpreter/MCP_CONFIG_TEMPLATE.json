{
  "mcp_servers": {
    "agentcore-gateway": {
      "url": "http://127.0.0.1:8080/mcp",
      "bearer_token_env_var": "BIFROST_MCP_VIRTUAL_KEY",
      "enabled": true,
      "startup_timeout_sec": 300,
      "tool_timeout_sec": 300
    }
  },
  "_agentcore": {
    "renderer": "open-interpreter",
    "source_contract": "contracts/agentcore-gateway-client.json",
    "live_target": "C:\\Users\\ynotf\\.openinterpreter\\config.toml",
    "notes": [
      "CLI Open Interpreter uses TOML [mcp_servers.<name>] with bearer_token_env_var — not JSON mcpServers.",
      "GUI Interpreter (%APPDATA%\\interpreter\\config.json) has no MCP schema — unsupported for gateway enrollment."
    ]
  },
  "_template_note": "Illustrative TOML-shaped template for CLI home config.toml. Apply with interpreter mcp add or direct TOML edit after backup."
}
