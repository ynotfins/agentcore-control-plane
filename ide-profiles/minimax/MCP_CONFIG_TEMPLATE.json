{
  "mcpServers": {
    "agentcore-gateway": {
      "type": "streamable-http",
      "url": "http://127.0.0.1:8080/mcp",
      "headers": {
        "Authorization": "Bearer ${env:BIFROST_MCP_VIRTUAL_KEY}"
      },
      "timeout": 300,
      "enabled": true,
      "configured": true
    }
  },
  "_agentcore": {
    "renderer": "minimax",
    "source_contract": "contracts/agentcore-gateway-client.json"
  },
  "_template_note": "Prefer streamable-http (MiniMax daemon uses Streamable HTTP). If live connection returns 401 virtual-key-required, MiniMax did not expand ${env:BIFROST_MCP_VIRTUAL_KEY} — materialize the User-scope VK into Authorization for the live mcp.json only (never commit the materialized value).",
  "_cli_limitation": "PATH wrappers mavis.cmd/minimax.cmd point at nonexistent resources/resources/daemon/cli.js on MiniMax Code 3.0.53.91 — CLI route unsupported; use in-app mcp.json only."
}
