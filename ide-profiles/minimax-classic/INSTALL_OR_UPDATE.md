# Install / Update — MiniMax Agent Classic

> **DERIVED FILE — do not hand-edit.** Generated by `scripts/render_ide_rules.py`.

Installation mode: **generated_prompt**. Live IDE files are never edited without explicit operator
authorization, a timestamped backup, and post-change validation.

## Rules delivery

1. Canonical semantics: `contracts/global-agent-policy.yaml` → rendered `ide-profiles/minimax-classic/GLOBAL_RULES.md`.
2. Delivery mechanism for this IDE: yes (direct_write; live edits remain approval-gated by policy) — target: C:\Users\ynotf\.minimax-agent\AGENT.md.
3. If delivery is `manual_import`/`generated_prompt`, hand the rendered GLOBAL_RULES.md content to the operator or paste it via the documented UI path. Do not skip rules silently.

## MCP configuration

1. Template: `ide-profiles/minimax-classic/MCP_CONFIG_TEMPLATE.json` (derived from `renderers/gateway-clients/minimax-classic.json`).
2. Live target: `cloud:matrix/api/v1/mcp/add_or_edit_server (no local mcp.json)` — exactly one `agentcore-gateway` entry.
3. Follow `docs/prompts/install-agentcore-gateway-in-ide.md` for the approved live-change procedure (backup first).
4. Env handling: unknown — Classic stores custom MCP via Matrix cloud API (server_name, base_url, mcp_server_type=UserCustomized); materialize bearer in UI if env expansion unsupported.
5. Restart behavior: restart Classic after MCP catalog or rules change.

## CLIENT-LOCAL EXECUTION SCOPE

The IDE running this prompt may inspect and modify only its own live
configuration, rules, agent settings, and backup.

Configuration examples for other IDEs are reference material only.

Do not inspect, back up, repair, restart, validate, or modify another IDE.

Cross-IDE reconciliation is a separate AgentCore control-plane task that
requires explicit operator authorization.

## After any change

Run the checks in `ide-profiles/minimax-classic/VALIDATION.md` and record the date in `IDE_PROFILE.yaml` (`last_validation_date`).
